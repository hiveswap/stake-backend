// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model RawStakeEvents {
  id        Int      @id @default(autoincrement())
  timestamp DateTime
  userAddr  String
  amount    String
  tokenAddr String
}

model RawAddLiquidityEvents {
  id        Int      @id @default(autoincrement())
  timestamp DateTime
  userAddr  String
  tokenX    String
  tokenY    String
  amountX   String
  amountY   String
}

model RawDecLiquidityEvents {
  id        Int      @id @default(autoincrement())
  timestamp DateTime
  userAddr  String
  tokenX    String
  tokenY    String
  amountX   String
  amountY   String
}

model Credits {
  userAddr  String   @id
  curCredit Int
  timestamp DateTime
}

model CreditHistory {
  id        Int      @id @default(autoincrement())
  userAddr  String
  timestamp DateTime
  credit    Int
}

model IndexedRecord {
  id          Int      @id @default(autoincrement())
  blockNumber Int
  updateTime  DateTime
}
